__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local databases and runtime state: never versioned.
*.db
*.db-wal
*.db-shm
.wasm-agent/

# Rust build output (host workspace and standalone plugin crates).
target/

# Secrets: never version these.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*credentials*.json
*credential*.json
*secret*.json
service-account*.json
id_rsa*
id_ed25519*
id_ecdsa*

# handoff.sh runtime output. The report and the run marker are per-run and must not be
# versioned; the baseline count is deliberately NOT ignored - property 1 needs a baseline
# that was reviewed, and a number nobody committed cannot be reviewed.
.handoff/last-report.txt
.handoff/running

# Scratch files: the probe scripts a run writes for itself. The convention is the mechanism - nothing here
# needs a rule in AGENTS.md, it needs a .gitignore line. The files stay on disk and stay usable.
tmp-*
